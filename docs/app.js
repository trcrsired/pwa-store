import './content.js';

window.addEventListener('load', () => {
  console.log('📦 PWA Store Loaded');

  // Optional future hooks
  // setupThemeToggle();
  // setupWrapperFilter();
  // setupSearchBar();
});
